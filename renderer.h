#include "vertexarray.h"
#include "indexbuffer.h"
#include "texture.h"
#include "shaders.h"

class Renderer
{
public:
    Renderer(VertexArray& vertexArray, IndexBuffer& indexBuffer, Texture& texture, Shader program);
private:
};